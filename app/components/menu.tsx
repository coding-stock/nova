'use client'
import { useState } from "react"
import { Activity } from "react"

function menu() {
    const [active , setActive] = useState('home');
  return (
    <div>menu</div>
  )
}

export default menu